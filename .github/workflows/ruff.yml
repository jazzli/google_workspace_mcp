name: Ruff

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-python@v6
      with:
        python-version: '3.11'
    - name: Install uv
      uses: astral-sh/setup-uv@v7
    - name: Validate
      # Use `uvx` so we run ruff in an isolated environment without invoking
      # the project's build backend or executing pyproject.toml hooks. This
      # keeps the validation step safe to run on untrusted fork PR code.
      # Pin the version established by uv.lock so a new Ruff release cannot
      # change the gate between otherwise identical workflow runs.
      run: |
        uvx --from ruff==0.15.10 ruff check
        uvx --from ruff==0.15.10 ruff format --check
