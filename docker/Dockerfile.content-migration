ARG RUNTIME_IMAGE
FROM ${RUNTIME_IMAGE}
COPY --chown=0:0 --chmod=0644 content_permission_migration.py /opt/mcp-runtime/content_permission_migration.py
USER 1000:1000
# Inherit R's safe run entrypoint. Migration requires an explicit root identity
# and replacement entrypoint with a separately approved, strictly bound manifest.
